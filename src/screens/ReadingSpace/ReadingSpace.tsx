import { SafeAreaView, StyleSheet, Text, View } from 'react-native';
import React from 'react';

const ReadingSpace = () => {
  return (
    <SafeAreaView>
      <Text>ReadingSpace</Text>
    </SafeAreaView>
  );
};

export default ReadingSpace;

const styles = StyleSheet.create({});
