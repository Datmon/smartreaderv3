export interface IBook {
  title: string;
  author: string;
  format: string;
  dateOfUploading: Date;
  dateOfTouched: Date;
  isLoaded: string;
  image: any;
  id: number;
  readed: number;
  pages: number;
}
